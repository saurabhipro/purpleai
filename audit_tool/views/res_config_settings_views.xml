<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.audit</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="90"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Audit Tool" string="Audit Tool" name="audit_tool">
                    <block title="Notifications &amp; Reminders">
                        <setting id="audit_reminders" help="Configure automated reminders for pending tasks.">
                            <field name="audit_reminder_enabled"/>
                            <div class="content-group" invisible="not audit_reminder_enabled">
                                <div class="row mt16">
                                    <label for="audit_reminder_frequency" class="col-lg-3 o_light_label"/>
                                    <field name="audit_reminder_frequency" class="oe_inline"/> days
                                </div>
                            </div>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="action_audit_config_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="context">{'module' : 'audit_tool'}</field>
    </record>

    <menuitem id="menu_audit_config" name="Configuration" parent="menu_audit_root" sequence="100" groups="group_audit_admin"/>
    <menuitem id="menu_audit_settings" name="Settings" parent="menu_audit_config" action="action_audit_config_settings" sequence="10"/>

</odoo>
