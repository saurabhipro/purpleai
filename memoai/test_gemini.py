import os, sys, json, requests

def main():
    try:
        # read odoo.conf via Python to get db pass etc is too complex,
        # but the key is just in the config parameter
        pass
    except Exception:
        pass
